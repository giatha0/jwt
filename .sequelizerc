const path = require('path');

module.exports = {
    'config': path.resolve('src/config', 'config.json'), // config file for sequelize cli
    'migrations-path': path.resolve('src/migrations', 'migrations'), // path to migrations folder
    'models-path': path.resolve('src/models', 'models'), // path to models folder
    'seeders-path': path.resolve('src/seeders', 'seeders') // path to seeders folder
}